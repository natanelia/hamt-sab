{
  "name": "zerocopy",
  "version": "0.1.0",
  "description": "Zero-copy immutable data structures for multi-threaded JavaScript via SharedArrayBuffer + WASM",
  "type": "module",
  "main": "shared.ts",
  "types": "shared.ts",
  "exports": {
    ".": {
      "bun": "./shared.ts",
      "import": "./shared.ts",
      "types": "./shared.ts"
    },
    "./tanstack": {
      "bun": "./tanstack-db-collection.ts",
      "import": "./tanstack-db-collection.ts",
      "types": "./tanstack-db-collection.ts"
    }
  },
  "scripts": {
    "build:wasm": "asc shared-map.as.ts -o shared-map.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads && asc shared-list.as.ts -o shared-list.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads && asc linked-list.as.ts -o linked-list.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads && asc singly-linked-list.as.ts -o singly-linked-list.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads && asc doubly-linked-list.as.ts -o doubly-linked-list.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads && asc ordered-map.as.ts -o ordered-map.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads && asc sorted-tree.as.ts -o sorted-tree.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads && asc priority-queue.as.ts -o priority-queue.wasm --importMemory --sharedMemory --initialMemory 2 --maximumMemory 65536 --enable threads",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "vitest run --config vitest.browser.config.ts",
    "bench": "bun benchmark.ts",
    "demo": "bun demo/serve.ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build:wasm && bun test"
  },
  "keywords": [
    "immutable",
    "shared",
    "wasm",
    "sharedarraybuffer",
    "worker",
    "hamt",
    "persistent",
    "data-structures",
    "tanstack-db",
    "bun"
  ],
  "author": "natanelia",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/natanelia/zerocopy.git"
  },
  "bugs": {
    "url": "https://github.com/natanelia/zerocopy/issues"
  },
  "homepage": "https://github.com/natanelia/zerocopy#readme",
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@vitest/browser": "^4.0.16",
    "@vitest/browser-playwright": "^4.0.16",
    "assemblyscript": "^0.28.9",
    "bun-types": "^1.2.17",
    "immutable": "^5.1.4",
    "playwright": "^1.57.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "peerDependencies": {
    "@tanstack/db": ">=0.5.0"
  },
  "peerDependenciesMeta": {
    "@tanstack/db": {
      "optional": true
    }
  }
}
